\ProvidesPackage{mystyle}

\usepackage{comment}

\usepackage{microtype}

% Beamer
\setbeamertemplate{navigation symbols}{}
\newenvironment{frame*}{\begin{frame}{\secname}{\subsecname}}{\end{frame}}
\usepackage{multicol}

% Minipage patch
\usepackage{xpatch}
\makeatletter
\xpatchcmd{\@iiiminipage}{%
  \let\@listdepth\@mplistdepth \@mplistdepth\z@
}{%
  \let\@listdepth\@mplistdepth
  \@mplistdepth\@enumdepth  % use the current depth (stored in \@enumdepth
}{\typeout{Patching minipage succeeded}}{\typeout{Patching failed}}% End of patching
\makeatother

%C++ logo
\usepackage{texlogos}

% Graphics
\input{graphics}

% Notes
\usetikzlibrary{tikzmark}
\colorlet{sidenote color}{blue!70!black!70}
\newcommand{\setsidenotecolor}[1][blue!70!black!70]{\colorlet{sidenote color}{#1}}
\newcommand<>*{\sidenote}[3][]{\tikzmarknode[#1]{mymark}{#2}\only#4{\begin{tikzpicture}[overlay,remember picture,shift=(mymark),sidenote color,-latex,font=\footnotesize]#3\end{tikzpicture}}}
\newcommand<>*{\sidenotehighlight}[2]{\sidenote#3[/visible on=#3,text opacity=1,fill=sidenote color!42,text height=height("I"),text depth=depth("g"),inner ysep=1]{#1}{#2}}

% Text formatting
\newcommand*{\defterm}[1]{\textbf{#1}}

% Algorithms
\usepackage[linesnumbered,tworuled]{algorithm2e}

% Maths
\newcommand*{\map}[3]{#1\colon#2\mathchoice{\longrightarrow}{\to}{\to}{\to}#3}
\newcommand*{\RR}{\mathbb{R}}
\newcommand*{\subs}{\subseteq}
\renewcommand*{\emptyset}{\varnothing}

% DeclareMathOperator
\DeclareMathOperator{\Star}{Star}
\DeclareMathOperator{\Link}{Link}